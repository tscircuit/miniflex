{
  "name": "@tscircuit/miniflex",
  "main": "dist/index.js",
  "type": "module",
  "version": "0.0.4",
  "scripts": {
    "format": "biome format --write .",
    "format:check": "biome format .",
    "typecheck": "tsc --noEmit",
    "build": "tsup ./lib/index.ts --dts --format esm --sourcemap inline"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@types/bun": "latest",
    "bun-match-svg": "^0.0.11",
    "graphics-debug": "^0.0.46",
    "tsup": "^8.4.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
